<div class="p-5 mb-4 bg-light rounded-3">
    <h1 class="display-5 fw-bold">{{ $title }}</h1>
    <p class="col-md-8 fs-5">{{ $short_description }}</p>
    <a href="{{ url('/product') }}" class="btn btn-primary btn-lg">Shop Now</a>
</div>